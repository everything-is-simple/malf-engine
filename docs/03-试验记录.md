# 03 — 试验记录

## 记录规则

这里记录机器实际运行结果；不能用“已安装”替代“能力通过”，也不能用“官方支持”替代本机 Windows Python 3.10 证据。

## 初始登记（实验启动前）

| ID | 组件/能力 | 初始状态 | 通过标准 |
|---|---|---|---|
| RB-FX-001 | Python 标准库 | candidate | 32 字节解析、坏 bar 整体拒绝、SHA-256、原子 current |
| RB-FX-002 | Pydantic v2 | candidate | 缺 lineage 拒绝、None + reason_codes、额外字段拒绝、frozen |
| RB-FX-003 | FastAPI + Uvicorn | candidate | GET/HEAD-only、本地绑定、无静默回退、刷新不写文件 |
| RB-FX-004 | pytest | candidate | 参数化/fixture/故障注入/非零失败码 |
| RB-FX-005 | Playwright Python | candidate | 三视口、无外部请求、刷新不触发构建 |
| RB-FX-006 | uv vs venv/pip | candidate | 可重建、锁定/离线策略、运行时不依赖 uv |
| RB-FX-007 | 图表候选 | candidate | 包大小、离线、可访问性、手机响应式、许可证证据 |

## 机器结果

实验完成后由脚本写入 `evidence/<id>/result.json`，并把摘要补在这里。若实验未运行，保持 candidate，不得凭预期填写通过。

## 机器实测结果（2026-07-19）

| ID | 结果 | 关键证据 | 结论 |
|---|---|---|---|
| RB-FX-001 | trial-passed | 7 个解析/拒绝/原子写入测试通过 | Python 标准库足以作为 Data/Core 底座候选 |
| RB-FX-002 | trial-passed | 5 个边界合同测试通过 | Pydantic 只放在边界，不进入 MALF 语义核心 |
| RB-FX-003 | trial-passed with limitation | 5 个 Viewer 合同测试通过 | FastAPI + Uvicorn 可作为只读 Viewer 候选；TestClient 有弃用警告，正式装配前处理 |
| RB-FX-004 | trial-passed | 故意失败用例返回非零退出码 | pytest 可承载 TDD 与故障注入 |
| RB-FX-005 | trial-passed | 三视口 Viewer + 无外部请求通过 | Playwright 仅作开发/验收依赖 |
| RB-FX-006 | trial-passed with limitation | uv lock/sync/run 成功 | uv 推荐为开发锁定工具；不成为生产运行时；存在跨盘 hardlink fallback 提示 |
| RB-FX-007 | trial-passed | ECharts、Lightweight Charts、原生 SVG 本地渲染通过 | v0.1 默认原生 SVG；两种库都 deferred，Lightweight Charts 是未来复杂金融图表首选候选 |

机器证据：`evidence/<experiment-id>/result.json`；汇总：`evidence/SUMMARY.json`。
