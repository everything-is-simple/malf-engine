---
name: malf-engine-current-status
description: malf-engine 当前开发状态与下一步行动
---

# malf-engine 当前状态

**更新日期**：2026-08-02  
**版本**：v2.1 with Structural Position  
**实现状态**：20/20 刀完成；实现完成不等于所有验证阶段均关闭。

## 1. 已确认的实现

- Core：pivot、guard、break、transition、bar_count、bar_index。
- Range：双边界、演化、resolution、continuation/reversal。
- Lifespan：Wave/Range `calculate_*`、`record_*`、历史读取。
- Rank：Wave/Range 双轨 peer sample、严格 percentile rank、`N=30` 样本门禁。
- Structural Position：P1–P4 builder。
- Service：`build_wave_structural_snapshot`、reason codes、用途/新鲜度门禁与 44 字段快照。

## 2. 当前回归测试

执行命令：

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:PYTHONPATH='Z:\ai-malf-riskbench-components\malf-engine;Z:\ai-malf-riskbench-components\malf-engine\src'
$env:MALF_TDX_ROOT='Z:\new_tdx64'
D:\miniconda\py310\python.exe -m pytest tests -q -p no:cacheprovider -rs
```

结果：

```text
109 passed in 1.23s
```

当前无 skip。原 `test_candidate_replacement_same_direction` 的 skip 是测试场景设计错误，已改为真实同向 refresh fixture。

审计报告：[`docs/.record/reports/VALIDATION-AUDIT-2026-08-02.md`](../.record/reports/VALIDATION-AUDIT-2026-08-02.md)

## 3. 验证阶段状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| V1 流水线 | ✅ 历史通过 | 旧流水线证据保留；不替代 T02 正式 driver |
| V2 结构正确性 | ✅ PASS | 15/15 人工案例；机器预检 38/38 pivot、4/4 break |
| V3 参数校准 | 🟡 部分完成 | 权威参数审计通过（`k=2`、`N=30`）；独立敏感性数据集尚未批准 |
| V4 公式核对 | ✅ PASS（代码合同层） | 方向对称 progress、Range resolution distance、rank 门禁均有确定性测试 |
| V5 bar_index | ✅ PASS | 0-based、单调递增、回归测试覆盖 |
| V6 文档治理 | 🟡 活动文档已刷新 | 当前状态和审计已更新；历史文件仍保留为历史证据 |

## 4. 与 T02 的边界

malf-engine 的 Lifespan、Rank、Structural Position、Service 实现已存在，且公开 API 回归通过。T02 不得再把“引擎尚未实现”写成阻塞理由。

但 T02 仍必须遵守公开事实边界：若 `CoreStateSnapshot` 没有公开给 driver 的完整 WaveLifecycleFacts 或 resolved Range facts，driver 不得读取私有状态、反推或补造这些事实；真实 TDX 输出应保持 `None + reason_codes`。

T02 synthetic fixture 已证明实际驱动顺序：

```text
Core → Lifespan（Wave + Range）→ Rank（Wave + Range）→ Structural Position（P1–P4）→ Service
```

## 5. 下一步

1. 为 V3 建立经批准的参数敏感性数据集；在此之前不把 V3 写成完整通过。V3–V6 属于 malf-engine 后续优化/验证事项，不构成 T02 当前关闭门禁。
2. T02 继续处理公开 lifecycle facts 的接入合同或诚实降级，不读取私有状态；这不是“malf-engine 未实现”，而是 adapter 对外事实合同尚未批准/形成。
3. 保持 `operational` 禁用，并将真实 TDX 与旧流水线证据严格分开。

**维护规则**：任何新记录都必须区分实现完成、单元测试通过、真实数据验证和人工验收，不得用“20/20”掩盖未完成门禁。
